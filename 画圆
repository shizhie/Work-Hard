#circle(img, center, radius, color[, thickness[, lineType[, shift]]]) -> img
#thickness：正数表示圆的厚度，负数表示要画一个完整的圆 

img = cv2.imread("AI.jpg",1)

img1 = cv2.circle(img,(np.int(img.shape[1]/2),np.int(img.shape[0]/2)),np.int(img.shape[0]/5),(255,255,255),-1)

cv2.imshow("img",img1)
cv2.waitKey(0)
cv2.destroyAllWindows()
